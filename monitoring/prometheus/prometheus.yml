global:
  scrape_interval: 5s

rule_files:
  - "alert-rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

scrape_configs:
  - job_name: 'webapp'
    static_configs:
      - targets: ['webapp:80']
    metrics_path: '/metrics'
    scrape_interval: 5s